# xhs_v4 账号同步爬虫服务接入说明

- 版本：`v1`
- 日期：`2026-04-11`
- 适用范围：`xhs_v4/crawler_service`

## 1. 目标

给 `xhs-v4` 提供一个独立的账号同步服务，用于接收报名人账号主页列表，返回：

1. `accounts`
2. `posts`
3. `snapshots`

主站 Worker 收到这些数据后，会直接导入账号看板，并自动回流到“报名人持续跟踪”统计。

## 2. 当前目录

新增目录：

- `crawler_service/main.py`
- `crawler_service/config.py`
- `crawler_service/schemas.py`
- `crawler_service/providers/mock_provider.py`
- `crawler_service/providers/playwright_xhs.py`
- `crawler_service/requirements.txt`
- `crawler_service/Dockerfile`

## 3. 当前支持的 Provider

### `mock`

默认模式，方便联调。

特点：

1. 不依赖真实小红书页面
2. 可直接返回模拟账号、笔记、快照
3. 适合先打通 `xhs-v4` 主站自动化链路

### `playwright_xhs`

预留真实抓取位。

特点：

1. 支持用 `Playwright` 打开账号主页
2. 通过环境变量配置页面选择器
3. 当前属于可落地骨架，仍需要你按真实页面补全/校准选择器

## 4. 接口

### 健康检查

`GET /healthz`

### 账号同步

`POST /xhs/account_posts`

请求示例：

```json
{
  "batch_name": "creator_sync_20260411_120000",
  "source_channel": "Crawler服务",
  "targets": [
    {
      "registration_id": 38,
      "submission_id": 13,
      "topic_id": 23,
      "creator_account_id": 5,
      "profile_url": "https://www.xiaohongshu.com/user/profile/xxx",
      "account_handle": "2060658648",
      "owner_name": "邹旦",
      "owner_phone": "13823816938"
    }
  ]
}
```

返回示例：

```json
{
  "success": true,
  "provider": "playwright_xhs",
  "batch_name": "creator_sync_20260411_120000",
  "source_channel": "Crawler服务",
  "accounts": [
    {
      "platform": "xhs",
      "account_handle": "2060658648",
      "display_name": "某账号",
      "profile_url": "https://www.xiaohongshu.com/user/profile/xxx",
      "follower_count": 1234
    }
  ],
  "posts": [
    {
      "platform": "xhs",
      "account_handle": "2060658648",
      "profile_url": "https://www.xiaohongshu.com/user/profile/xxx",
      "post_url": "https://www.xiaohongshu.com/discovery/item/abc123",
      "title": "新笔记",
      "publish_time": "2026-04-11 14:30:00",
      "views": 321,
      "likes": 45,
      "favorites": 12,
      "comments": 8
    }
  ],
  "snapshots": [
    {
      "platform": "xhs",
      "account_handle": "2060658648",
      "profile_url": "https://www.xiaohongshu.com/user/profile/xxx",
      "snapshot_date": "2026-04-11",
      "follower_count": 1234,
      "post_count": 18,
      "total_views": 9800,
      "total_interactions": 1300
    }
  ]
}
```

## 5. 本地启动

### 方式 A：先用 mock 联调

1. 执行 `crawler_service/scripts/bootstrap_venv.sh`
2. 执行 `scripts/run_crawler_dev.sh`
3. 设置主站自动化中心：
   - `账号同步模式 = remote`
   - `账号同步 API URL = http://127.0.0.1:8081/xhs/account_posts`

### 方式 B：切到 Playwright

1. 设置 `CRAWLER_PROVIDER=playwright_xhs`
2. 准备登录态 `PLAYWRIGHT_STORAGE_STATE_PATH`
3. 按真实页面配置：
   - `XHS_POST_CARD_SELECTOR`
   - `XHS_POST_LINK_SELECTOR`
   - `XHS_POST_TITLE_SELECTOR`
   - `XHS_PROFILE_NAME_SELECTOR`
   - `XHS_FOLLOWER_COUNT_SELECTOR`
4. 再启动 crawler 服务

## 6. Docker Compose

仓库根目录 `docker-compose.yml` 已增加 `crawler` 服务。

如果使用容器内网地址，主站建议配置：

- `CREATOR_SYNC_API_URL=http://crawler:8081/xhs/account_posts`

## 7. 建议推进顺序

1. 先用 `mock` 跑通主站账号同步任务
2. 再切 `playwright_xhs`
3. 先只抓标题、链接、点赞/评论这些基础字段
4. 页面选择器稳定后，再补粉丝、曝光、更多统计

## 8. 备注

当前 `playwright_xhs` 是“最小可接入骨架”，已经能承接真实抓取逻辑，但真实选择器、登录态和页面结构适配仍需要根据你实际账号页面校准。
